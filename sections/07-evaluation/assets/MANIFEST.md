# Asset Manifest

This directory will contain only presentation assets derived from the new
experiment:

- `end-to-end.svg`: the final unified performance chart;
- HTML player metadata linking the paired quality-suite MP4s.

Raw data and MP4 files live under `experiments/` so their protocol and
provenance remain independent of article layout.
